-i

#Configures the download directory
-o D:/Video-Archive/"%(uploader)s/%(title)s-%(id)s.%(ext)s"

# Archive Settings (insures no duplicates will be downloaded)
--download-archive downloaded-video-id-archive.txt

#Reads Channel URLs From File
-a yt-dlp-channels.txt

# Uniform mp4 Format
-f mp4

# Gets all auto-generated subs, converts them to an SRT file
#--write-auto-subs
#--embed-subs

# Gets metadata
--embed-metadata
--write-description

# Debug
-v
